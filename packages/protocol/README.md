# protocol

API schemas and contracts for Hermes Local Studio.

Contains:
- `openapi.yaml` — Adapter API specification (all `/shell/*` endpoints)
- `events.schema.json` — Normalized event types
- `theme.schema.json` — Theme pack TOML schema
- `layout.schema.json` — Layout pack TOML schema

These will be defined in Phase 1.
